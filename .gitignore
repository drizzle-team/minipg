node_modules

# issue-cache local data
issue-cache/.pgdata/
issue-cache/corpus.json
issue-cache/*.jsonl
.DS_Store

# local test postgres
test/.pgdata/

# build output
dist/
*.tgz

# local env / secrets (AWS creds, Aurora Data API config, etc.)
.env
.env.local
.env.*.local

# SST — generated platform, cache, logs, deploy state (regenerated by `sst install`/`sst deploy`)
.sst/
sst-env.d.ts
